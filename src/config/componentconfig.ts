import { ComponentMapType } from '../../lib/@typedefs';
import { InputComponent, SelectComponent } from '../components';

export const componentMap: ComponentMapType = {
    INPUT: InputComponent,
    SELECT: SelectComponent,
};
